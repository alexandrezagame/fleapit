// find the card that was clicked on

window.location.replace("<%= potential_matches_item_path(@my_item) %>");
// document.querySelector("[data-item-id='<%= @not_wanted_item.id %>']").remove();

// remove the html of the card
